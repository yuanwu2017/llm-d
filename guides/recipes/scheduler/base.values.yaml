## Base values shared by both standalone and inferencepool (gateway) charts.
## Layer chart-specific or guide-specific overrides on top with additional -f flags.

inferenceExtension:
  replicas: 1
  image:
    hub: ghcr.io/llm-d
    name: llm-d-inference-scheduler
    tag: v0.7.0
  flags:
    v: 2
